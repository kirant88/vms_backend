# Django and Core Dependencies
setuptools>=68.0.0
Django==4.2.7
openpyxl>=3.1.2

# Async Task Processing
celery==5.3.4
redis==5.0.1
django-celery-beat==2.5.0
django-celery-results==2.5.1

# Database
# SQLite is included with Python (no extra driver needed)
# For production PostgreSQL (commented out for local dev on Windows to avoid build tools requirement):
# psycopg2-binary==2.9.9

# Production Server
gunicorn==21.2.0
whitenoise==6.6.0

# Environment Variables
python-dotenv==1.0.0

# CORS and Security
django-environ==0.11.2

# Development Dependencies (optional)
# django-debug-toolbar==4.2.0
# django-extensions==3.2.3